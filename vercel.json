{
  "version": 2,
  "builds": [
    {
      "src": "stripe-server.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/server.js" }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "stripe-server.js"
    }
  ]
}
